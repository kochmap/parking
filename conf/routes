# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /                                                                controllers.ParkingMeterController.index
POST        /parking-meters/:parkingMeterId/start                            controllers.ParkingMeterController.startParkingMeter(parkingMeterId)
GET         /parking-meters/:parkingMeterId/stop                             controllers.ParkingMeterController.stopParkingMeter(parkingMeterId)
GET         /vehicles/:vehicleLicensePlateId/has-active-parking-meter        controllers.VehicleController.hasVehicleHasActiveParkingMeter(vehicleLicensePlateId)
POST        /parking-tickets/:ticketId/charge-fee                            controllers.ParkingTicketController.chargeTicketFee(ticketId)
GET         /parking-tickets/:ticketId/fee                                   controllers.ParkingTicketController.getTicketFee(ticketId)
GET         /fees                                                            controllers.FeeController.earningsDuring(date)