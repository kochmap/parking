# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /                                            controllers.ParkingController.index
POST        /parking-meter/:parkingMeterId/start        controllers.ParkingController.startParkingMeter(parkingMeterId)
GET         /parking-meter/:parkingMeterId/stop         controllers.ParkingController.stopParkingMeter(parkingMeterId)