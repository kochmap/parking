# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /                                                               controllers.ParkingMeterController.index
POST        /parking-meter/:parkingMeterId/start                            controllers.ParkingMeterController.startParkingMeter(parkingMeterId)
GET         /parking-meter/:parkingMeterId/stop                             controllers.ParkingMeterController.stopParkingMeter(parkingMeterId)
GET         /vehicle/:vehicleLicensePlateId/has-active-parking-meter        controllers.VehicleController.hasVehicleHasActiveParkingMeter(vehicleLicensePlateId)
POST        /parking-ticket/:ticketId/charge-fee                            controllers.ParkingTicketController.checkoutTicket(ticketId)
GET         /parking-ticket/:ticketId/fee                                   controllers.ParkingTicketController.getTicketFee(ticketId)